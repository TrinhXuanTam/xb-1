image: python:latest

services:
  selenium:
    image: selenium/standalone-chrome
    ports:
      - "4444:4444"

before_script:
  - python -V
  - pip install -r requirements.txt

stages:
  - test

test:
  stage: test
  script:
  - cd xb1
  - python ./manage.py test