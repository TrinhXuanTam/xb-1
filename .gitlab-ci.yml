image: python:latest

services:
  - selenium/standalone-chrome

before_script:
  - python -V
  - pip install -r requirements.txt

stages:
  - test

test:
  stage: test
  script:
  - cd xb1
  - python ./manage.py test